import React, { Component } from 'react'

export default class AdminFeature extends Component {
    render() {
        return (
            <div>
                Admin Feature : Available for admin-only.
            </div>
        )
    }
}
